import numpy as np
import random
import cv2
import time
import math
import matplotlib.pyplot as plt

# image = cv2.imread("./123.jpg")
# trans = np.fft.fft2(image)
# trans = np.fft.fftshift(trans)
# trans = np.log(1 + np.abs(trans))
# print(trans)
# cv2.imshow("origin",image)
# cv2.imshow("freq",trans)
# cv2.waitKey()

for i in range(20,0,-1):
    print(i)